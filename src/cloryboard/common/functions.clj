(ns cloryboard.common.functions
  (:require [clojure.pprint :as pp]
            [clojure.edn :as edn]
            [clojure.set :as set]
            [clojure.string :as str]
            [clojure.java.io :as io]))

;;Common utils used by functions that can be written here.

(defn get-current-object-position
  []
  ()
